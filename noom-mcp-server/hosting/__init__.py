# Databricks Apps hosting layer — HTTP serving infrastructure for the hosted deployment.
# This package is only used by run_app.py (the hosted entrypoint).
# Local development uses run.py (stdio) and does not import this package.
