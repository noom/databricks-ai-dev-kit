version: 2
updates:
  # Keep GitHub Actions pinned SHAs up to date
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

  # databricks-builder-app is upstream code — Noom does not maintain it.
  # Dependabot for that directory is intentionally omitted.
