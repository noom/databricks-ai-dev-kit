name: CI

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

# This file is inherited from upstream (databricks-solutions/ai-dev-kit) and
# kept as a no-op to avoid merge conflicts on upstream syncs.
# Noom-owned CI lives in noom-mcp-server.yml.
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "upstream CI placeholder — Noom CI is in noom-mcp-server.yml"
